set shiftwidth=2   " Use 2 spaces when auto indenting.
set softtabstop=2  " Use 2 spaces when hitting tab, deleting etc.

" Set guide bar to 80 characters (seems to be common for shell scripts).
if exists('&colorcolumn')
    set colorcolumn=80
endif
